// tailwind.config.js

module.exports = {
  // other Tailwind CSS configuration options...

  // Configure the content sources
  content: [
    './src/**/*.{html,js,jsx,ts,tsx}', // Scan HTML, JSX, JavaScript, and TypeScript files in the src directory and its subdirectories
    // Add more paths as needed for your project
  ],
}

